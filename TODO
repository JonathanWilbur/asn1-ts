- Make `let`s `const` where possible
- Use `===` where possible
- Use unsigned shifts where possible
- Use different numeric literal appropriately
- Fix infinite recursion vulnerabilities
- Suggest improvements to TypeScript
  - unsigned integers
  - Recursive `const`
  - No-op warnings or failures